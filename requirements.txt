# ============================================================
# 导弹动力智能评估 — Python 依赖
# pip install -r requirements.txt
# ============================================================

# Web 框架 + 服务器
fastapi>=0.100,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9

# 环境变量
python-dotenv>=1.0,<2

# LLM / VLM API 调用
openai>=2.0,<3
httpx>=0.27,<1

# 数值计算与数据处理
numpy>=1.24,<3
pandas>=2.0,<3

# 图表与可视化
plotly>=5.0,<7
matplotlib>=3.7,<4
pyvista>=0.42,<1

# Fluent HDF5 后处理
h5py>=3.8,<4

# ============================================================
# 重构新增（评估平台 v2）
# ============================================================
# 数据库（评估元数据持久化）
sqlalchemy>=2.0,<3
# 判据/映射 YAML（sim-knowledge）
pyyaml>=6.0,<7
# DeepAgents 架构（主编排 agent + 子 agent + tools + 规划/虚拟FS）
deepagents>=0.1
langchain>=0.3
langgraph>=0.2
langchain-openai>=0.2
# 异步文件
aiofiles>=23.0
# 测试
pytest>=8.0
pytest-asyncio>=0.23
